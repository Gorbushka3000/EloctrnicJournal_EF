<Window x:Class="EloctrnicJournal_EF.View.RatingsStudentWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EloctrnicJournal_EF.View"
        mc:Ignorable="d"
        Title="RatingsStudentWindow" Height="550" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.5*"/>
            <RowDefinition Height="4*"/>
            <RowDefinition Height="0.5*"/>
        </Grid.RowDefinitions>
        <ListBox Grid.Row="1" x:Name="studentList" ItemsSource="{Binding Grade}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <TextBlock Text="{Binding LessonId.LessonName}" FontSize="16" />
                        <ListBox>
                            <StackPanel Orientation="Vertical">
                                <TextBlock Text="{Binding Grades}" FontSize="14"/>
                            </StackPanel>
                        </ListBox>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>
